spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm.jdbc.bind: TRACE

jwt:
  secret_key: master-share-jwt-secret-master-share-jwt-secret
